services:
  app:
    build: .
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8080", "--reload"]
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
    volumes:
      - ./app:/app/app  # hot reload during local dev
